import React from "react";

import style from "./ThisDayInfo.module.scss";

type Props = {};

const ThisDayInfo = (props: Props) => {
  return <div className={style.home}>ThisDayInfo</div>;
};

export default ThisDayInfo;
